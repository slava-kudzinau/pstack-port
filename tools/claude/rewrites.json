{
  "rewrites": [
    {
      "source": "Substituting `generalPurpose` skips that read and drifts.",
      "replacement": "Substituting a different agent type skips that read and drifts."
    },
    {
      "source": "Use your configured architect runners (defaults `claude-fable-5-1-thinking-max`, `gpt-5.6-sol-max`, `grok-4.6-fast-xhigh`, `claude-opus-5-thinking-xhigh`).",
      "replacement": "Use your configured architect runners (defaults: the models you configure per role)."
    },
    {
      "source": "Use `arena runners` from `~/.cursor/rules/pstack-models.mdc` when present.",
      "replacement": "Use the `arena runners` entries from `~/.claude/pstack-models.md`, imported from `CLAUDE.md` as `@pstack-models.md` when present."
    },
    {
      "source": "Otherwise default to one each on `claude-fable-5-1-thinking-max`, `gpt-5.6-sol-max`, `grok-4.6-fast-xhigh`, `claude-opus-5-thinking-xhigh`.",
      "replacement": "Otherwise default to one runner per model you have configured, per the `arena runners` entries above."
    },
    {
      "source": "After all Phase B candidates complete, choose one model from the `arena cross-judge pool` in `~/.cursor/rules/pstack-models.mdc` when present.",
      "replacement": "After all Phase B candidates complete, choose one model from the `arena cross-judge pool` entries in `~/.claude/pstack-models.md`, imported from `CLAUDE.md` as `@pstack-models.md` when present."
    },
    {
      "source": "Otherwise use `claude-fable-5-1-thinking-max`, `gpt-5.6-sol-max`, `grok-4.6-fast-xhigh`, `claude-opus-5-thinking-xhigh`.",
      "replacement": "Otherwise use the cross-judge models you configured under `arena cross-judge pool`."
    },
    {
      "source": "This skill orchestrates three others: an inline mining pass (see step 1), Cursor's built-in `create-skill` (authoring), and the **unslop** skill (prose discipline).",
      "replacement": "This skill orchestrates three others: an inline mining pass (see step 1), the plugin's own authoring playbook at `playbooks/authoring-a-skill.md`, and the **unslop** skill (prose discipline)."
    },
    {
      "source": "The system prompt names the workspace's `agent-transcripts/` directory.",
      "replacement": "Claude Code keeps each project's transcripts under `~/.claude/projects/<slug>/`, newest first."
    },
    {
      "source": "Don't glob across `~/.cursor/projects/*/`.",
      "replacement": "Don't glob across `~/.claude/projects/*/`."
    },
    {
      "source": "Use Cursor's built-in `create-skill` skill to author the skill.",
      "replacement": "Author it with the plugin's own `playbooks/authoring-a-skill.md` playbook."
    },
    {
      "source": "- Cursor's built-in `create-skill` skill: skill authoring process and writing guidelines.",
      "replacement": "- `playbooks/authoring-a-skill.md`: skill authoring process and writing guidelines."
    },
    {
      "source": "- `subagent_type`: `generalPurpose`",
      "replacement": "- `subagent_type`: `general-purpose`"
    },
    {
      "source": "- `model`: your configured how-explorer model (default `grok-4.6-fast-xhigh`)",
      "replacement": "- `model`: your configured how-explorer model"
    },
    {
      "source": "- `model`: your configured how-explainer model (default `claude-fable-5-1-thinking-max`)",
      "replacement": "- `model`: your configured how-explainer model"
    },
    {
      "source": "After the explanation is complete, spawn one architectural critic per model in your configured how-critics list (defaults `claude-fable-5-1-thinking-max`, `gpt-5.6-sol-max`, `grok-4.6-fast-xhigh`, `claude-opus-5-thinking-xhigh`), all in a single message.",
      "replacement": "After the explanation is complete, spawn one architectural critic per model in your configured how-critics list (defaults: the models you configure per role), all in a single message."
    },
    {
      "source": "Use the `interrogate reviewers` list from `~/.cursor/rules/pstack-models.mdc` when present, one reviewer per entry, extending or shrinking the Reviewer A/B/C/D labels below to the configured entry count; otherwise use the table defaults.",
      "replacement": "Use the `interrogate reviewers` entries in `~/.claude/pstack-models.md`, imported from `CLAUDE.md` as `@pstack-models.md` when present, one reviewer per entry, extending or shrinking the Reviewer A/B/C/D labels below to the configured entry count; otherwise use one reviewer per model you have available."
    },
    {
      "source": "| Reviewer A | `claude-fable-5-1-thinking-max` |",
      "replacement": "| Reviewer A | `interrogate reviewers` entry 1 |"
    },
    {
      "source": "| Reviewer B | `gpt-5.6-sol-max` |",
      "replacement": "| Reviewer B | `interrogate reviewers` entry 2 |"
    },
    {
      "source": "| Reviewer C | `grok-4.6-fast-xhigh` |",
      "replacement": "| Reviewer C | `interrogate reviewers` entry 3 |"
    },
    {
      "source": "| Reviewer D | `claude-opus-5-thinking-xhigh` |",
      "replacement": "| Reviewer D | `interrogate reviewers` entry 4 |"
    },
    {
      "source": "Agent-facing prose also follows the **create-skill** skill (Cursor's built-in for authoring SKILL.md files).",
      "replacement": "Agent-facing prose also follows the plugin's own `playbooks/authoring-a-skill.md` playbook for authoring `SKILL.md` files."
    },
    {
      "source": "- Before commit \u2192 the `deslop` skill from the `cursor-team-kit` plugin (`/deslop`).",
      "replacement": "- Before commit \u2192 the **deslop** skill (`/deslop`), which this plugin ships."
    },
    {
      "source": "`cursor-team-kit` publishes `control-cli` (CLIs and TUIs) and `control-ui` (browser / Electron / web UIs).",
      "replacement": "No shipped built-in drives a surface here, so use the repo's own verification skill, and `create-verification-skill` generates one that drives CLIs and TUIs at the terminal and browser / Electron / web UIs in the browser."
    },
    {
      "source": "- Any PR-status request \u2192 the **Babysit** playbook (`playbooks/babysit.md`), and not Cursor's built-in babysit skill, whose description matches the same words.",
      "replacement": "- Any PR-status request \u2192 the **Babysit** playbook (`playbooks/babysit.md`). No built-in carries that name, so nothing else competes for the same wording."
    },
    {
      "source": "**Defaults for every `Agent` call.** `run_in_background: true`, agent mode (readonly strips MCP), file pointers not inlined context, explicit model per role (configurable via `/setup-pstack`; defaults `grok-4.6-fast-xhigh` for code, `claude-fable-5-1-thinking-max` for prose and judgment).",
      "replacement": "**Defaults for every `Agent` call.** `run_in_background: true`, agent mode (readonly strips MCP), file pointers not inlined context, explicit model per role (configurable via `/setup-pstack`; the per-role config supplies each role's model)."
    },
    {
      "source": "The hardest changes (cross-cutting design, gnarly concurrency, subtle algorithms) go to your strongest judgment model (`claude-fable-5-1-thinking-max`) when the task needs judgment or the intent is vague, and to your strongest instruction-following model (`claude-fable-5-1-thinking-max`) when the work is a precisely specified sequence of steps to execute to the letter; trivial mechanical edits go to your fast code model.",
      "replacement": "The hardest changes (cross-cutting design, gnarly concurrency, subtle algorithms) go to your strongest judgment model when the task needs judgment or the intent is vague, and to your strongest instruction-following model when the work is a precisely specified sequence of steps to execute to the letter; trivial mechanical edits go to your fast code model."
    },
    {
      "source": "- **Pause safely.** Suspending in-flight work cleanly so it can be resumed, on an explicit pause, going offline, a Cursor restart, or imminent context compaction.",
      "replacement": "- **Pause safely.** Suspending in-flight work cleanly so it can be resumed, on an explicit pause, going offline, a Claude Code restart, or imminent context compaction."
    },
    {
      "source": "Use the **create-skill** skill (Cursor's built-in for authoring SKILL.md files).",
      "replacement": "Use the plugin's own `playbooks/authoring-a-skill.md` playbook for authoring `SKILL.md` files."
    },
    {
      "source": "Pick the wake mechanism using Cursor's `/loop` command (a built-in, not a pstack skill).",
      "replacement": "Pick the wake mechanism using Claude Code's `/loop` command (a built-in, not a pstack skill)."
    },
    {
      "source": "On that go, arm a `/goal` with the full program objective.",
      "replacement": "On that go, write the full program objective into the standing orders and restate it in your todolist."
    },
    {
      "source": "One Cursor cloud agent per PR owns build, the first push, a ready PR, self-proof on the real artifact (the **prove-it-works** principle skill), skeptical Bugbot triage per `../references/bugbot-triage.md`, a slop-strip (the `deslop` skill from the `cursor-team-kit` plugin (`/deslop`)), `/no-comments` (the **no-comments** skill), a rebase onto current trunk, the babysit loop to green (`playbooks/babysit.md`), and the merge itself.",
      "replacement": "One background subagent per PR, isolated in its own worktree, owns build, the first push, a ready PR, self-proof on the real artifact (the **prove-it-works** principle skill), skeptical Bugbot triage per `../references/bugbot-triage.md`, a slop-strip (the **deslop** skill, `/deslop`), `/no-comments` (the **no-comments** skill), a rebase onto current trunk, the babysit loop to green (`playbooks/babysit.md`), and the merge itself."
    },
    {
      "source": "The lanes: re-run the gates at that SHA; prove the load-bearing behavior live on the real surface the change touches (`control-cli` or `control-ui` from `cursor-team-kit` as the change demands); audit the receipts and the diff, distrusting the PR body.",
      "replacement": "The lanes: re-run the gates at that SHA; prove the load-bearing behavior live on the real surface the change touches (CLIs and TUIs at the terminal, browser / Electron / web UIs in the browser, or through the repo's own verification skill, as the change demands); audit the receipts and the diff, distrusting the PR body."
    },
    {
      "source": "A cloud root uses the existing cloud-sleeper wake chain instead.",
      "replacement": "No remote root runs this port, so the local cadence above is the only one."
    },
    {
      "source": "At each tick, re-read this playbook from trunk with `git show origin/main:pstack/skills/poteto-mode/playbooks/autopilot-full.md`, then re-read the armed `/goal`.",
      "replacement": "At each tick, re-read this playbook from disk (`skills/poteto-mode/playbooks/autopilot-full.md` under the installed plugin), then re-read the standing objective."
    },
    {
      "source": "One Cursor cloud agent per PR owns its change end to end: build, first push, a ready PR opened before self-proof, self-proof (gates, CI, receipts), skeptical Bugbot triage per `../references/bugbot-triage.md`, a slop-strip (the `deslop` skill from the `cursor-team-kit` plugin (`/deslop`)), `/no-comments` (the **no-comments** skill), and babysit to green per `playbooks/babysit.md`.",
      "replacement": "One background subagent per PR, isolated in its own worktree, owns its change end to end: build, first push, a ready PR opened before self-proof, self-proof (gates, CI, receipts), skeptical Bugbot triage per `../references/bugbot-triage.md`, a slop-strip (the **deslop** skill, `/deslop`), `/no-comments` (the **no-comments** skill), and babysit to green per `playbooks/babysit.md`."
    },
    {
      "source": "At each tick, re-read this playbook from trunk with `git show origin/main:pstack/skills/poteto-mode/playbooks/autopilot-stack.md`, then re-read the armed `/goal`.",
      "replacement": "At each tick, re-read this playbook from disk (`skills/poteto-mode/playbooks/autopilot-stack.md` under the installed plugin), then re-read the standing objective."
    },
    {
      "source": "On her explicit go, arm a `/goal` with the full program objective.",
      "replacement": "On her explicit go, write the full program objective into the standing orders and restate it in your todolist."
    },
    {
      "source": "This playbook replaces Cursor's built-in babysit skill for these requests, so do not route there even though its description matches the same words.",
      "replacement": "No built-in carries this name, so this playbook is the only route for these requests, and its description matches the same words."
    },
    {
      "source": "Drive a long or stubborn hunt with Cursor's `/loop` command.",
      "replacement": "Drive a long or stubborn hunt with Claude Code's `/loop` command."
    },
    {
      "source": "Delegate implementation to a subagent using your configured bug-fix model (default `claude-fable-5-1-thinking-max`) with a specific scope; review the diff.",
      "replacement": "Delegate implementation to a subagent using your configured bug-fix model with a specific scope; review the diff."
    },
    {
      "source": "**Verify the chain from transcripts, not self-report.** Read each candidate's local transcript under the active workspace's `agent-transcripts/` directory (the system prompt names this path).",
      "replacement": "**Verify the chain from transcripts, not self-report.** Read each candidate's local transcript under `~/.claude/projects/<slug>/`, where the newest `.jsonl` is the live session."
    },
    {
      "source": "Do not glob across `~/.cursor/projects/*/`; that crosses workspace boundaries and reads private chats from unrelated projects.",
      "replacement": "Do not glob across `~/.claude/projects/*/`; that crosses workspace boundaries and reads private chats from unrelated projects."
    },
    {
      "source": "Delegate code-writing to a subagent using your configured feature model (default `grok-4.6-fast-xhigh`) with a specific scope (file paths, named data shape and its organizing structure per **principle-model-the-domain** \u2014 a state machine over scattered booleans, a table/registry over branching, a typed model over repeated shape assumptions, chosen before the delegate writes logic \u2014 and success criteria); review its diff yourself.",
      "replacement": "Delegate code-writing to a subagent using your configured feature model with a specific scope (file paths, named data shape and its organizing structure per **principle-model-the-domain** \u2014 a state machine over scattered booleans, a table/registry over branching, a typed model over repeated shape assumptions, chosen before the delegate writes logic \u2014 and success criteria); review its diff yourself."
    },
    {
      "source": "   - Hand the change to a subagent using your configured hillclimb model (default `claude-fable-5-1-thinking-max`) with a tight scope; supervise and review the diff rather than typing it (the **guard-the-context-window** principle skill).",
      "replacement": "   - Hand the change to a subagent using your configured hillclimb model with a tight scope; supervise and review the diff rather than typing it (the **guard-the-context-window** principle skill)."
    },
    {
      "source": "Ten lanes on `grok-4.6-fast-xhigh` at the PR head drive the real surface through its control skill, per the **swarm** skill.",
      "replacement": "Ten lanes on the worker model you configured under `swarm workers`, at the PR head, drive the real surface through its control skill, per the **swarm** skill."
    },
    {
      "source": "Browser, Electron, and web UIs use `control-ui` from `cursor-team-kit`.",
      "replacement": "Browser, Electron, and web UIs are driven in the browser."
    },
    {
      "source": "CLIs and TUIs use `control-cli` from `cursor-team-kit`.",
      "replacement": "CLIs and TUIs are driven at the terminal."
    },
    {
      "source": "- [ ] On her go, arm a `/goal` with this exact text.",
      "replacement": "- [ ] On her go, write this exact text into the standing orders."
    },
    {
      "source": "  - [ ] `git show origin/main:pstack/skills/poteto-mode/playbooks/<execution playbook>.md`",
      "replacement": "    - [ ] `skills/poteto-mode/playbooks/<execution playbook>.md` under the installed plugin"
    },
    {
      "source": "  - [ ] `git show origin/main:pstack/skills/swarm/SKILL.md`",
      "replacement": "    - [ ] `skills/swarm/SKILL.md` under the installed plugin"
    },
    {
      "source": "  - [ ] `git show origin/main:pstack/skills/poteto-mode/playbooks/opening-a-pr.md`",
      "replacement": "    - [ ] `skills/poteto-mode/playbooks/opening-a-pr.md` under the installed plugin"
    },
    {
      "source": "  - [ ] `git show origin/main:pstack/skills/<each other leaf skill the program uses>`",
      "replacement": "    - [ ] `skills/<each other leaf skill the program uses>` under the installed plugin"
    },
    {
      "source": "In a cloud root, a cloud-sleeper wake chain.",
      "replacement": "No remote root exists in this port, so only the local wake applies."
    },
    {
      "source": "\"Re-read the execution playbook from trunk and the armed /goal.",
      "replacement": "\"Re-read the execution playbook from disk and the standing objective."
    },
    {
      "source": "Each live lane runs on its own cloud VM at the PR head.",
      "replacement": "Each live lane runs in its own worktree at the PR head."
    },
    {
      "source": "Drive through `control-ui` or `control-cli` from `cursor-team-kit`.",
      "replacement": "Drive it in the browser for UIs and at the terminal for CLIs and TUIs."
    },
    {
      "source": "Ten lanes on `grok-4.6-fast-xhigh` at the PR head, per the boot recipe.",
      "replacement": "Ten lanes on the worker model you configured under `swarm workers`, at the PR head, per the boot recipe."
    },
    {
      "source": "**PRs.** Run `/deslop` from `cursor-team-kit` over the diff before commit.",
      "replacement": "**PRs.** Run `/deslop` over the diff before commit."
    },
    {
      "source": "Name the real path, such as `control-cli`, `control-ui`, or the targeted tests.",
      "replacement": "Name the real path, such as driving the terminal, driving the browser, or the targeted tests."
    },
    {
      "source": "- **Worker / verifier.** Always `environment: \"cloud\"` unless the task needs this machine: `control-ui` or `control-cli` runtime verification (from `cursor-team-kit`); reading local transcripts under `agent-transcripts/`; simulators and local IDE state; auth that exists only here.",
      "replacement": "- **Worker / verifier.** Workers run on this machine, one writer per own worktree or branch, unless the task needs something that only exists here: runtime verification in the browser or at the terminal, or through the repo's own verification skill; reading local transcripts under `~/.claude/projects/`; simulators and local IDE state; auth that exists only here."
    },
    {
      "source": "Cloud agents cannot read the local store, so their briefs inline what they need or point at repo paths.",
      "replacement": "Workers share this machine, so their briefs still inline what they need or point at repo paths."
    },
    {
      "source": "Probe read-only: the ledger, `units.tsv`, `gh`, pushed branches, the cloud agent's status in the Cursor dashboard.",
      "replacement": "Probe read-only: the ledger, `units.tsv`, `gh`, pushed branches, and the live agent list behind `ListAgents`."
    },
    {
      "source": "- After a Cursor restart: local agents are dead, cloud work is not.",
      "replacement": "- After a Claude Code restart: local subagents are dead, pushed branches are not."
    },
    {
      "source": "Leave a checkpoint a cold-start agent can resume from.** For \"pause safely\", \"I need to go offline\", \"restart Cursor\", or \"board my flight\", and when context is about to compact or summarize.",
      "replacement": "Leave a checkpoint a cold-start agent can resume from.** For \"pause safely\", \"I need to go offline\", \"restart Claude Code\", or \"board my flight\", and when context is about to compact or summarize."
    },
    {
      "source": "Delegate implementation to a subagent using your configured perf-issue model (default `claude-fable-5-1-thinking-max`); review the diff.",
      "replacement": "Delegate implementation to a subagent using your configured perf-issue model; review the diff."
    },
    {
      "source": "Delegate the mechanical edits to a subagent using your configured refactoring model (default `grok-4.6-fast-xhigh`) with a specific scope (file paths, the names being moved, the behavior to hold); review the diff yourself.",
      "replacement": "Delegate the mechanical edits to a subagent using your configured refactoring model with a specific scope (file paths, the names being moved, the behavior to hold); review the diff yourself."
    },
    {
      "source": "A local transcript under the active workspace's `agent-transcripts/` directory (the system prompt names the path; do not glob across `~/.cursor/projects/*/`, that crosses workspace boundaries and reads private chats from unrelated projects), a cloud-agent URL, or a pushed branch.",
      "replacement": "A local transcript under `~/.claude/projects/<slug>/` (the newest `.jsonl` is the live session; do not glob across `~/.claude/projects/*/`, that crosses workspace boundaries and reads private chats from unrelated projects), or a pushed branch."
    },
    {
      "source": "One subagent per PR, not batched, each a Cursor cloud agent, each exercising the real surface (`control-ui` or `control-cli` from `cursor-team-kit` as the change demands) against parent versus head.",
      "replacement": "One subagent per PR, not batched, each isolated in its own worktree, each exercising the real surface (the browser for UIs, the terminal for CLIs and TUIs, or the repo's own verification skill, as the change demands) against parent versus head."
    },
    {
      "source": "It reads paths from `git worktree list`, never hand-typed, since a hand-typed `myrepo-worktrees/x` misses one that lives at `.cursor/worktrees/myrepo/x` (principle-encode-lessons-in-structure).",
      "replacement": "It reads paths from `git worktree list`, never hand-typed, since a hand-typed `myrepo-worktrees/x` misses one that lives under a nested worktree directory such as `.worktrees/myrepo/x` (principle-encode-lessons-in-structure)."
    },
    {
      "source": "More when needed: Xcode `DerivedData` and `iOS DeviceSupport`; `~/Library/Application Support/Cursor` (`state.vscdb.backup`, and `snapshots/roots/<root>` where a `<root>` named for a folder you opened as a workspace balloons); package caches (pnpm, uv, brew, yarn).",
      "replacement": "More when needed: Xcode `DerivedData` and `iOS DeviceSupport`; the editor's own application-support directory (`~/Library/Application Support/<editor>`, holding `state.vscdb.backup` and `snapshots/roots/<root>` where a `<root>` named for a folder you opened as a workspace balloons); package caches (pnpm, uv, brew, yarn)."
    },
    {
      "source": "Transcripts live at `~/.cursor/projects/<slug>/agent-transcripts/<uuid>/<uuid>.jsonl`, where `<slug>` is the workspace path with the leading slash dropped and each \"/\" turned into \"-\" (so `/Users/you/proj` becomes `Users-you-proj`).",
      "replacement": "Transcripts live at `~/.claude/projects/<slug>/<uuid>.jsonl`, where `<slug>` is the workspace path with each `/` and each `_` turned into `-` (so `/Users/you/proj` becomes `-Users-you-proj`)."
    },
    {
      "source": "The system prompt names the active workspace's `agent-transcripts/` directory; use that path.",
      "replacement": "Claude Code keeps the active workspace's transcripts under `~/.claude/projects/<slug>/`; use that path."
    },
    {
      "source": "Do not glob across `~/.cursor/projects/*/`.",
      "replacement": "Do not glob across `~/.claude/projects/*/`."
    },
    {
      "source": "ls -t <agent-transcripts>/*.jsonl <agent-transcripts>/*/*.jsonl <agent-transcripts>/*/subagents/*.jsonl 2>/dev/null | head -10",
      "replacement": "ls -t ~/.claude/projects/<slug>/*.jsonl 2>/dev/null | head -10"
    },
    {
      "source": "One message, three `Agent` calls, `subagent_type: generalPurpose`, explicit `model:` on each, agent mode (`readonly: false`).",
      "replacement": "One message, three `Agent` calls, `subagent_type: general-purpose`, explicit `model:` on each, agent mode (`readonly: false`)."
    },
    {
      "source": "| Judgment | your configured reflect-judgment model (default `claude-fable-5-1-thinking-max`) | `references/judgment-reviewer.md` |",
      "replacement": "| Judgment | your configured reflect-judgment model | `references/judgment-reviewer.md` |"
    },
    {
      "source": "| Tooling | your configured reflect-tooling model (default `gpt-5.6-sol-max`) | `references/tooling-reviewer.md` |",
      "replacement": "| Tooling | your configured reflect-tooling model | `references/tooling-reviewer.md` |"
    },
    {
      "source": "| Divergent | your configured reflect-judgment model (default `claude-fable-5-1-thinking-max`) | `references/divergent-reviewer.md` |",
      "replacement": "| Divergent | your configured reflect-judgment model | `references/divergent-reviewer.md` |"
    },
    {
      "source": "One `Agent` call, `subagent_type: generalPurpose`, using your configured reflect-judgment model (default `claude-fable-5-1-thinking-max`), agent mode (`readonly: false`).",
      "replacement": "One `Agent` call, `subagent_type: general-purpose`, using your configured reflect-judgment model, agent mode (`readonly: false`)."
    },
    {
      "source": "- Substantive existing-skill edit (a new section, a new pattern table, more than ~10 lines): hand to Cursor's built-in `create-skill` skill and run its draft / test / iterate loop.",
      "replacement": "- Substantive existing-skill edit (a new section, a new pattern table, more than ~10 lines): hand to a subagent running the plugin's own `playbooks/authoring-a-skill.md` playbook and run its draft / test / iterate loop."
    },
    {
      "source": "- `Read` tool calls against any `SKILL.md` file (workspace `.claude/skills/`, user-level `~/.claude/skills/`, or plugin-installed paths under `~/.cursor/plugins/`)",
      "replacement": "- `Read` tool calls against any `SKILL.md` file (workspace `.claude/skills/`, user-level `~/.claude/skills/`, or plugin-installed paths under `~/.claude/plugins/`)"
    },
    {
      "source": "- \"we renamed `gpt-4` to `gpt-4o` in `encodingForModel`\"",
      "replacement": "- \"we renamed `<old-id>` to `<new-id>` in `encodingForModel`\""
    },
    {
      "source": "Write `~/.cursor/rules/pstack-models.mdc`, an always-applied rule that sets pstack's model per role.",
      "replacement": "Write `~/.claude/pstack-models.md`, imported from `CLAUDE.md` as `@pstack-models.md`, a memory file that sets pstack's model per role."
    },
    {
      "source": "If Cursor also exposes a models API or CLI that lists the user's entitled models, prefer it for completeness.",
      "replacement": "If Claude Code's `/model` picker lists the models this account is entitled to, prefer it for completeness."
    },
    {
      "source": "If `~/.cursor/rules/pstack-models.mdc` already exists, read it and treat its values as the current choices.",
      "replacement": "If `~/.claude/pstack-models.md`, imported from `CLAUDE.md` as `@pstack-models.md` already exists, read it and treat its values as the current choices."
    },
    {
      "source": "Write `~/.cursor/rules/pstack-models.mdc` with `alwaysApply: true` and one line per role, using the same labels poteto-mode uses.",
      "replacement": "Write `~/.claude/pstack-models.md` with one line per role, imported from `CLAUDE.md` as `@pstack-models.md` so it reaches every new session, using the same labels poteto-mode uses."
    },
    {
      "source": "feature, refactoring: grok-4.6-fast-xhigh",
      "replacement": "feature, refactoring: <detected-fast-code-slug>"
    },
    {
      "source": "bug-fix: claude-fable-5-1-thinking-max",
      "replacement": "bug-fix: <detected-judgment-slug>"
    },
    {
      "source": "perf-issue: claude-fable-5-1-thinking-max",
      "replacement": "perf-issue: <detected-judgment-slug>"
    },
    {
      "source": "hillclimb: claude-fable-5-1-thinking-max",
      "replacement": "hillclimb: <detected-judgment-slug>"
    },
    {
      "source": "judgment and prose: claude-fable-5-1-thinking-max",
      "replacement": "judgment and prose: <detected-judgment-slug>"
    },
    {
      "source": "hardest tasks: claude-fable-5-1-thinking-max",
      "replacement": "hardest tasks: <detected-judgment-slug>"
    },
    {
      "source": "how explorer: grok-4.6-fast-xhigh",
      "replacement": "how explorer: <detected-fast-code-slug>"
    },
    {
      "source": "how explainer: claude-fable-5-1-thinking-max",
      "replacement": "how explainer: <detected-judgment-slug>"
    },
    {
      "source": "how critics: claude-fable-5-1-thinking-max, gpt-5.6-sol-max, grok-4.6-fast-xhigh, claude-opus-5-thinking-xhigh",
      "replacement": "how critics: <detected-judgment-slug>, <detected-tooling-slug>, <detected-fast-code-slug>, <detected-heavy-slug>"
    },
    {
      "source": "why investigators: grok-4.6-fast-xhigh",
      "replacement": "why investigators: <detected-fast-code-slug>"
    },
    {
      "source": "why synthesizer: claude-fable-5-1-thinking-max",
      "replacement": "why synthesizer: <detected-judgment-slug>"
    },
    {
      "source": "reflect tooling: gpt-5.6-sol-max",
      "replacement": "reflect tooling: <detected-tooling-slug>"
    },
    {
      "source": "reflect judgment, divergent, synthesizer: claude-fable-5-1-thinking-max",
      "replacement": "reflect judgment, divergent, synthesizer: <detected-judgment-slug>"
    },
    {
      "source": "arena runners: claude-fable-5-1-thinking-max, gpt-5.6-sol-max, grok-4.6-fast-xhigh, claude-opus-5-thinking-xhigh",
      "replacement": "arena runners: <detected-judgment-slug>, <detected-tooling-slug>, <detected-fast-code-slug>, <detected-heavy-slug>"
    },
    {
      "source": "arena cross-judge pool: claude-fable-5-1-thinking-max, gpt-5.6-sol-max, grok-4.6-fast-xhigh, claude-opus-5-thinking-xhigh",
      "replacement": "arena cross-judge pool: <detected-judgment-slug>, <detected-tooling-slug>, <detected-fast-code-slug>, <detected-heavy-slug>"
    },
    {
      "source": "swarm workers: grok-4.6-fast-xhigh",
      "replacement": "swarm workers: <detected-fast-code-slug>"
    },
    {
      "source": "architect runners: claude-fable-5-1-thinking-max, gpt-5.6-sol-max, grok-4.6-fast-xhigh, claude-opus-5-thinking-xhigh",
      "replacement": "architect runners: <detected-judgment-slug>, <detected-tooling-slug>, <detected-fast-code-slug>, <detected-heavy-slug>"
    },
    {
      "source": "interrogate reviewers: claude-fable-5-1-thinking-max, gpt-5.6-sol-max, grok-4.6-fast-xhigh, claude-opus-5-thinking-xhigh",
      "replacement": "interrogate reviewers: <detected-judgment-slug>, <detected-tooling-slug>, <detected-fast-code-slug>, <detected-heavy-slug>"
    },
    {
      "source": "Read this run's transcript under the active workspace's `agent-transcripts/` directory (the system prompt names the path).",
      "replacement": "Read this run's transcript under `~/.claude/projects/<slug>/`, where the newest `.jsonl` is this session."
    },
    {
      "source": "Don't glob across `~/.cursor/projects/*/`; that reads unrelated private chats.",
      "replacement": "Don't glob across `~/.claude/projects/*/`; that reads unrelated private chats."
    },
    {
      "source": "Pick the worker model from `swarm workers` in `~/.cursor/rules/pstack-models.mdc` when present.",
      "replacement": "Pick the worker model from the `swarm workers` entry in `~/.claude/pstack-models.md`, imported from `CLAUDE.md` as `@pstack-models.md` when present."
    },
    {
      "source": "Otherwise use `grok-4.6-fast-xhigh`.",
      "replacement": "Otherwise use the worker model you configured under `swarm workers`."
    },
    {
      "source": "Spawn all N workers in one message with `subagent_type: generalPurpose`, `environment: \"cloud\"`, `run_in_background: true`, and the configured model.",
      "replacement": "Spawn all N workers in one message with `subagent_type: general-purpose`, `run_in_background: true`, and the configured model, each in its own worktree."
    },
    {
      "source": "Before spawning investigators, list the available MCPs from the Cursor environment.",
      "replacement": "Before spawning investigators, list the available MCP servers that this session exposes."
    },
    {
      "source": "Otherwise inspect the `mcps/` directory Cursor exposes for enabled MCP servers.",
      "replacement": "Otherwise inspect the MCP servers this project's own settings enable."
    },
    {
      "source": "- `model`: your configured why-investigators model (default `grok-4.6-fast-xhigh`)",
      "replacement": "- `model`: your configured why-investigators model"
    },
    {
      "source": "- `model`: your configured why-synthesizer model (default `claude-fable-5-1-thinking-max`)",
      "replacement": "- `model`: your configured why-synthesizer model"
    },
    {
      "source": "alwaysApply: true",
      "replacement": "# imported from CLAUDE.md; Claude Code has no auto-applied rules directory"
    },
    {
      "source": "description: pstack per-role model choices (overrides skill defaults)",
      "replacement": "description: pstack per-role model choices (overrides what the skills say inline)"
    },
    {
      "source": "const LANES = \"Ten lanes on `grok-4.6-fast-xhigh` at the PR head\";",
      "replacement": "const LANES = \"Ten lanes on the worker model you configured under `swarm workers`, at the PR head\";"
    },
    {
      "source": "const PROGRAM_MARKERS = [\"/goal\", \"git show origin/main:\", /30[- ]minute/, \"status message\"];",
      "replacement": "const PROGRAM_MARKERS = [\"/loop\", \"git show origin/main:\", /30[- ]minute/, \"status message\"];"
    },
    {
      "source": "# Transcripts dir: ~/.cursor/projects/<slugified-repo-path>/agent-transcripts.\nslug=$(printf '%s' \"$main_wt\" | sed 's#^/##; s#/#-#g')\ntranscripts=\"$HOME/.cursor/projects/$slug/agent-transcripts\"",
      "replacement": "command -v jq >/dev/null || echo \"warn: jq not found; PR column will be empty\" >&2\ncommand -v rg >/dev/null || echo \"warn: rg not found; LAST_CHAT column will be empty and no row can bucket verify-recent-chat\" >&2\n\n# Transcripts: ~/.claude/projects/<encoded-cwd>/<uuid>.jsonl, where <encoded-cwd> is a\n# session's cwd with every \"/\" turned into \"-\". A session run inside a worktree lives\n# under that worktree's own directory, so scan the whole projects tree, not one repo's.\ntranscripts=\"$HOME/.claude/projects\"",
      "kind": "block"
    },
    {
      "source": "Spawn a single Task subagent that explores",
      "replacement": "Spawn a single subagent that explores"
    },
    {
      "source": "spawn a single Task subagent to synthesize",
      "replacement": "spawn a single subagent to synthesize"
    },
    {
      "source": "(omit Task `model`)",
      "replacement": "(omit `model` from the call)"
    },
    {
      "source": "the full Task schema including",
      "replacement": "the full Agent-tool schema including"
    }
  ]
}
